1) go through all sub-directories and follow README
	This will install the required docker images
2) run
	docker-compose up
or (as daemon):
	docker-compose up -d

TODO:
- TLS certificates
- set passwords in some kind of secrets file with env variables?
